{
  "name": "cardnial-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "watch:eleventy": "npx @11ty/eleventy --serve",
	  "watch:sass": "npx sass src:_site/css --watch",
	  "start": "npm run watch:eleventy & npm run watch:sass"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.81.0"
  }
}
